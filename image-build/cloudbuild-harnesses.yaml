# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Cloud Build configuration for building just the harness images
# This builds on top of the existing scion-base:<_TAG> image (defaults to scion-base:latest)
# Use this when you only need to update harness-specific configuration (claude, gemini, etc.)
steps:
  # Pre-flight: verify push access to catch permission errors early
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'verify-registry'
    entrypoint: 'bash'
    args: ['image-build/scripts/verify-registry.sh', '$_REGISTRY']

  # Enable multi-arch builds
  - name: 'gcr.io/cloud-builders/docker'
    id: 'setup-buildx'
    args: ['buildx', 'create', '--name', 'mybuilder', '--use']
    env:
      - 'DOCKER_CLI_EXPERIMENTAL=enabled'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'bootstrap-buildx'
    args: ['buildx', 'inspect', '--bootstrap']
    env:
      - 'DOCKER_CLI_EXPERIMENTAL=enabled'

  # Build Gemini Harness Image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-scion-gemini'
    dir: 'image-build/gemini'
    args:
      - 'buildx'
      - 'build'
      - '--platform'
      - 'linux/amd64,linux/arm64'
      - '--build-arg'
      - 'BASE_IMAGE=$_REGISTRY/scion-base:$_TAG'
      - '-t'
      - '$_REGISTRY/scion-gemini:$_SHORT_SHA'
      - '-t'
      - '$_REGISTRY/scion-gemini:$_TAG'
      - '-f'
      - 'Dockerfile'
      - '--pull'
      - '--push'
      - '.'
    env:
      - 'DOCKER_CLI_EXPERIMENTAL=enabled'

  # Build Claude Harness Image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-scion-claude'
    dir: 'image-build/claude'
    args:
      - 'buildx'
      - 'build'
      - '--platform'
      - 'linux/amd64,linux/arm64'
      - '--build-arg'
      - 'BASE_IMAGE=$_REGISTRY/scion-base:$_TAG'
      - '-t'
      - '$_REGISTRY/scion-claude:$_SHORT_SHA'
      - '-t'
      - '$_REGISTRY/scion-claude:$_TAG'
      - '-f'
      - 'Dockerfile'
      - '--pull'
      - '--push'
      - '.'
    env:
      - 'DOCKER_CLI_EXPERIMENTAL=enabled'

  # Build OpenCode Harness Image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-scion-opencode'
    dir: 'harnesses/opencode'
    args:
      - 'buildx'
      - 'build'
      - '--platform'
      - 'linux/amd64,linux/arm64'
      - '--build-arg'
      - 'BASE_IMAGE=$_REGISTRY/scion-base:$_TAG'
      - '-t'
      - '$_REGISTRY/scion-opencode:$_SHORT_SHA'
      - '-t'
      - '$_REGISTRY/scion-opencode:$_TAG'
      - '-f'
      - 'Dockerfile'
      - '--pull'
      - '--push'
      - '.'
    env:
      - 'DOCKER_CLI_EXPERIMENTAL=enabled'

  # Build Codex Harness Image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-scion-codex'
    dir: 'harnesses/codex'
    args:
      - 'buildx'
      - 'build'
      - '--platform'
      - 'linux/amd64,linux/arm64'
      - '--build-arg'
      - 'BASE_IMAGE=$_REGISTRY/scion-base:$_TAG'
      - '-t'
      - '$_REGISTRY/scion-codex:$_SHORT_SHA'
      - '-t'
      - '$_REGISTRY/scion-codex:$_TAG'
      - '-f'
      - 'Dockerfile'
      - '--pull'
      - '--push'
      - '.'
    env:
      - 'DOCKER_CLI_EXPERIMENTAL=enabled'

substitutions:
  _REGISTRY: 'us-central1-docker.pkg.dev/${PROJECT_ID}/public-docker'
  _TAG: 'latest'
options:
  dynamicSubstitutions: true
  machineType: 'E2_HIGHCPU_8'
timeout: 1800s # 30 minutes - harnesses are lightweight
